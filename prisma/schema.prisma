// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL_PRO")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  userId       String  @id @default(auto()) @map("_id") @db.ObjectId
  userEmail    String  @unique
  userName     String?
  userUsername String
  userPassword String
  userAvatar   String?
}

model Product {
  productId             String   @id @default(auto()) @map("_id") @db.ObjectId
  productName           String   @unique
  productDescription    String
  productCostPrice      Int
  productPrice          Int
  productFinalPrice     Int
  productVariants       Json
  productDiscountType   String
  productDiscountAmount Int?
  productMemo           String
  productCategory       String
  productSubCategory    String
  productImages         String[]
}

model Category {
  categoryId      String   @id @default(auto()) @map("_id") @db.ObjectId
  categoryName    String   @unique
  categorySubName String[]
}

// model Cart {
//   cartId  String        @id @default(auto()) @map("_id") @db.ObjectId
//   Product CartProduct[]
// }

// model CartProduct {
//   cartProductId    String  @id @default(auto()) @map("_id") @db.ObjectId
//   productId        String
//   productName      String
//   productVariant   Variant @relation(fields: [variantVariantId], references: [variantId])
//   Cart             Cart?   @relation(fields: [cartCartId], references: [cartId])
//   cartCartId       String? @db.ObjectId
//   variantVariantId String  @db.ObjectId
// }

// model Variant {
//   variantId   String        @id @default(auto()) @map("_id") @db.ObjectId
//   color       String[]
//   size        String
//   CartProduct CartProduct[]
// }
